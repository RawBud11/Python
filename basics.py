# Perform the arithmetic operation
result = ((3 + 2) / 2.5) ** 2

# Create a string that includes the arithmetic operation and its result
operation = "((3 + 2) / 2.5) ** 2"
output = f"The arithmetic operation {operation} equals {result}"

# Display the output
print(output)